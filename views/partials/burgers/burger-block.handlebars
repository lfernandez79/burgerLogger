<li class="mt-3">
    {{burger_name}}

    {{#if devour}}
    <button class=" btn btn-outline-primary text-light change-devour" data-id="{{id}}" data-nowdevour="{{devour}}">Devour! </button>
        {{else}}
    <button class="btn btn-outline-primary text-light delete-burger" data-id="{{this.id}}">Delete</button>
    {{/if}}

</li>